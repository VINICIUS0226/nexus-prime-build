services:
  - type: web
    name: nexus-prime
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    disk:
      name: build_cache
      mountPath: /opt/render/.cache
      sizeGB: 3
